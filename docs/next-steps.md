# Next Steps

1. Run the backend:
   - cd backend
   - npm install
   - npm start

2. Open `frontend/index.html` in the browser and click "Get Recommendations".

3. Tasks to continue:
   - Add unit tests for `rewardsService` (node + jest)
   - Add data validation on the backend (AJV or manual checks)
   - Implement persistent storage (SQLite) if desired
   - Expand frontend flow and add client-side form validation
   - Add README badges and developer notes
